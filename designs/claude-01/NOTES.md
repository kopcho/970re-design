# claude-01 — Home + Bottom Nav + Re970 (Parcel Discovery)

Claude's app-shell pass. Interaction skeleton for Grok's visual system to sit in.

## Bottom nav (5 tabs, Re970 = raised center signature)
`Search · Map · ◆Re970 · Saved · Rich`
- Re970 is elevated + spark-orange (`#E85D04`) with a lot-line polygon glyph — the moat made the literal centerpiece.
- Inline SVG icons; active = forest `#1A4A32`, inactive = mist.

## Home
Sparse forest search-hero ("Know the 970.") → one search field + 2 example chips → Re970 teaser strip → "Featured · Rich's listings" evidence rail (real gems + verbatim quotes).

## Re970 / Parcel view (what the center tab opens)
Satellite aerial + lot lines over the trees · address search + geolocate · satellite/street toggle · you-are-here pulse · draggable parcel card:
- **Open:** acres, zoning, year built, parcel #, lot dimensions.
- **Gated ("Agent only"):** owner of record + exact measured boundaries, blurred → "Text Rich to unlock" (lead-gen).

Aerial is a canvas stand-in for real satellite tiles + Regrid lot lines.
