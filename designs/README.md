# designs/

Drop each design pass in its own folder — e.g. `grok-01/`, `grok-02/`.
Anything goes: screenshots (`.jpg/.png`), clickable HTML mockups, tokens, notes.
Claude Code reads from here to implement the winning direction onto the live engine.
